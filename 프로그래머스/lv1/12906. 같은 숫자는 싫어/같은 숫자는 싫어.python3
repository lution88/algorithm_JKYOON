def solution(arr):
    answer = []
    for i in range(len(arr)):
        try:
            if arr[i] != arr[i+1]:
                answer.append(arr[i])
            else:
                continue
        except:
            answer.append(arr[i])
    return answer