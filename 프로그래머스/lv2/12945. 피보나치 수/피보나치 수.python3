def solution(n):
    i = 1
    previous = 0 
    current = 1 

    while i < n:
        c = previous
        previous = current
        current = c + previous
        answer = current
        i += 1
    return current % 1234567